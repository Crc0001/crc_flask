# HwishAI 菌种识别系统 · 双端部署架构说明

> 交付形态：**同一套代码，两种运行模式（vendor / client）**。
> 模型与知识库数据永远只在我们本地（vendor），客户机器只跑纯业务 Web 系统（client）。

## 1. 总体架构

```
┌─ 客户内网（我们远程安装，无模型 / 无知识库数据）─────────────────────┐
│                                                                   │
│  客户浏览器 ──▶ Flask Web 系统（APP_ROLE=client，端口 8856）        │
│  客户系统   ──▶ /api/v1/*（账号 API Token）                        │
│                  │                                                │
│                  ├─ 登录 / 账户管理（本地 MySQL：sys_user）         │
│                  ├─ 功能2 菌种数据库（本地 MySQL：sample 等业务表）  │
│                  ├─ 功能3 趋势分析（本地 MySQL）                    │
│                  ├─ 功能1 菌种检测页 ──▶ 转发我方模型 API ─────┐    │
│                  └─ 功能4 知识库页   ──▶ 转发我方知识库 API ────┼─┐  │
│                                                                 │ │  │
└─────────────────────────────────────────────────────────────────┼─┼──┘
                                                          HTTPS    │ │
┌─ 我方机房（vendor，模型与数据资产所在地）──────────────────────────▼─▼──┐
│  云中继（轻量云服务器 frp/Caddy，只转发流量，不落模型/数据）            │
│    │  + API Token 鉴权 + IP 白名单                                    │
│    ▼                                                                  │
│  Flask 模型/知识库服务（APP_ROLE=vendor，端口 8355）                   │
│    ├─ POST /api/v1/recognize   → YOLO + BioCLIP + XGBoost 识别管线    │
│    └─ GET  /api/v1/knowledge/* → BacDive 知识库（我方 MySQL crc_ai）   │
└────────────────────────────────────────────────────────────────────────┘
```

## 2. 两端职责

| 项目 | vendor（我方，模型机房） | client（客户本地） |
|---|---|---|
| APP_ROLE | `vendor` | `client` |
| 端口 | 8355（可配） | 8856（可配） |
| 模型（YOLO/BioCLIP/XGBoost） | ✅ 本地加载 | ❌ 不安装、不导入 |
| torch / ultralytics / opencv | ✅ | ❌（不装依赖） |
| 知识库数据（bacdive/silva/strain） | ✅ 我方 MySQL | ❌ 不部署、查询走 API |
| 业务数据（账号/样品/检测记录/趋势） | ✅ | ✅ 客户本地 MySQL |
| 功能1 菌种检测 | 本地管线直接跑 | 页面代理 → 转发我方 recognize API |
| 功能4 知识库 | 本地查询 | 页面代理 → 转发我方 knowledge API |
| 登录 / 账户管理 | ✅ | ✅（super_admin 账号我们远程可管全部） |
| MALDI-TOF 质谱/16S 匹配 | ✅（嵌入菌种检测页，无独立页面；参考谱库在我方，批量维护走 scripts/import_maldi_data.py） | ❌（客户版检测页相应面板需隐藏，见交付注意） |

## 3. 端口与网络

- **8355**：我方模型/知识库 API 对外端口（避开 80/443/22/3306/8080/5000 等常用端口）。
- **8856**：客户本地 Web 服务端口（客户内网访问）。
- 客户机 → 我方：仅出站 HTTPS 到云中继；无需入站开放。
- 我方 → 客户：远程桌面工具（向日葵/ToDesk）运维。

## 4. 核心数据流

### 4.1 功能1 菌种检测（识别）

```
客户浏览器上传图片
  → client:/api/orb_detect（校验登录会话）
  → client 以机器令牌转发 → vendor:POST /api/v1/recognize
  → vendor 本地跑完整管线（裁皿 → 切片选块 → 分类聚合 → 整皿覆盖）
  → 返回 Top3 + 知识库记录ID + 结果图（base64）
  → client 落盘结果图到本地 static/results，改写知识库链接为本机代理页
  → 浏览器展示 Top3；录入样品走本地 MySQL；PDF 报告本地生成
```

### 4.2 功能4 菌种知识库（查询）

```
客户浏览器搜索/查看详情
  → client:/strain_showcase/*（代理路由）
  → client 以机器令牌转发 → vendor:GET /api/v1/knowledge/search|detail
  → vendor 查我方 crc_ai 库（bacdive_record / silva 等）
  → JSON 回传 → client 用轻量模板渲染（数据不落客户库）
```

### 4.3 登录与远程调用

- 页面访问：Flask-Login 会话（全局拦截，除登录页外全部要求登录）。
- 客户系统调用 client 的 `/api/v1/*`：`Authorization: Bearer <账号API Token>`。
- client 调用 vendor：`Authorization: Bearer <机器令牌>`（写在 client 的 instance/config.py，vendor 侧白名单校验）。

## 5. 代码双模式（关键文件）

| 文件 | 说明 |
|---|---|
| `app/config.py` | 读取环境变量 + `instance/config.py`；`HWISHAI_APP_ROLE=vendor/client` |
| `app/__init__.py` | 按角色注册蓝图、全局登录拦截、初始账号引导 |
| `app/services/recognition.py` | **vendor 专用**识别管线（裁皿/切片/聚合/整皿覆盖） |
| `app/services/report_pdf.py` | 检测报告 PDF（双端共用，纯业务） |
| `app/routes/ai_detection.py` | 功能1 路由：vendor 走本地管线；client 走代理 |
| `app/routes/strain_showcase.py` | 功能4 路由：vendor 查本地库；client 走代理 |
| `app/routes/remote_api.py` | `/api/v1/*` 统一入口（两端行为自动切换） |
| `app/routes/auth.py` / `admin_accounts.py` | 登录、改密、账户管理 |
| `app/models/user.py` | 账号表 `sys_user` + 审计表 `sys_audit_log` |

## 6. 账号与权限

- 角色：`super_admin`（厂家内置 `hwishai`，客户管理员不可见）→ `admin`（客户管理员）→ `operator`（操作员）。
- 客户 admin 只能管理 operator；我们 super_admin 远程登录后能管理客户全部账号（含客户自建）。
- 密码哈希存储；连续失败 5 次锁定 10 分钟；admin 首次登录强制改密；SECRET_KEY 每台随机生成。
- 每个账号可签发一个 API Token（只存哈希），供客户系统调用 `/api/v1/*`。

### 管理控制台（仅管理员角色可见，侧边栏"管理控制台"入口）

前端页面 ↔ 后端接口 ↔ MySQL 全链路（`sys_user` / `sys_role_permission` / `sys_audit_log`）：

| 页面 | 功能 |
|---|---|
| `/admin/` 概览 | 账号/样品/近7天检测/MALDI 参考谱统计 + 最近登录 |
| `/admin/accounts` 用户管理 | 增删改查、启停、重置密码、角色、API Token 签发 |
| `/admin/permissions` 权限管理 | 按角色勾选功能权限，保存即生效 |
| `/admin/logs` 操作日志 | 登录/账号/权限操作审计，支持筛选分页 |

功能权限码：`ai_detection` 菌种检测 / `strain_db` 菌种数据库 / `analysis` 趋势分析 /
`strain_showcase` 知识库 / `maldi_matching` MALDI-TOF匹配（仅 vendor）/ `api_access` 远程调用API。
super_admin 恒为全部权限；admin/operator 权限存 `sys_role_permission`（首次启动按全量种子初始化）。
侧边栏菜单按权限显示，路由与业务 API 统一拦截（403），`/api/v1/*` 按账号权限校验 `api_access`。
账户管理本身不属功能授权范围，仅管理员角色可进。

### 审计边界（谁能看到什么日志）

- **客户本地日志管理**（客户库 `sys_audit_log`）：客户的全部业务操作——登录/退出、
  提交识别（detect_submit）、报告入库（report_save）、样品编辑/删除（sample_edit/delete）、
  导出 PDF、账号/权限变更、客户系统经 API 的调用（api_recognize/api_knowledge_*）。
  这些只写在客户本地库，**我方看不到**。
- **我方日志管理**（我方库 `sys_audit_log`）：我方本地操作 + 对客户开放的 API 调用层记录——
  哪个客户机器、何时调用识别/知识库、识别 Top1 与置信度（api_recognize/api_knowledge_*）、
  无效令牌尝试（api_auth_failed）。**我方只能看到 API 层，看不到客户入库与业务数据。**
- 结论：客户"检测了一个菌"会同时出现在客户日志（业务操作）和我方日志（API 调用）里；
  客户"录入了数据"只出现在客户日志里，我方看不到。

## 7. 远程调用 API（统一 JSON，Bearer Token）

| 方法 | 路径 | 说明 | 部署位置 |
|---|---|---|---|
| POST | `/api/v1/recognize` | 上传图片 → 菌种 Top3（multipart，字段名 `image`） | vendor 实跑 / client 转发 |
| GET | `/api/v1/knowledge/search` | 知识库检索：`q`、`domain`、`type_strain`、`genus`、`page`、`per_page` | vendor 实查 / client 转发 |
| GET | `/api/v1/knowledge/<record_id>` | 菌种知识库详情 | vendor 实查 / client 转发 |

错误约定：HTTP 401 = 令牌无效；HTTP 200 且 `success=false` = 业务失败（`message` 说明原因）。

## 8. 部署要点（Phase 4，另行执行）

- 客户机：**全新空库初始化**——只建表结构，不导任何业务数据（交付后样品/菌种库/检测记录/趋势均为空）。
  账号现场创建：`hwishai`（厂家专用，密码我方持有）+ 客户管理员账号（用户名/密码由客户当场设置，首登强制改密），操作员由客户管理员后续自行创建。
- MySQL 8G 调优：客户机 4核8G，部署时把 InnoDB buffer pool（256~512M）等内存参数调小，避免 MySQL 吃满内存。
- 启动：waitress（非 Flask dev server）+ NSSM 注册 Windows 服务开机自启（崩溃自动重启）；防火墙放行内网 8856；
  用户零操作：双击桌面快捷方式即打开系统。
- **客户库每日自动备份**：Windows 计划任务（02:30）跑 `backup_db.py`，mysqldump 到 `C:\HwishAI\backups\`，
  保留 30 天并写 `backup.log`。
- **半自动部署工具包** `deploy_client/`：00 下载 vendor 载荷（Python 安装包/NSSM/离线 wheel）→ build 组装
  `dist/hwishai_client_deploy/` → 客户机上 01~05 脚本依次执行（装 Python → venv 离线装依赖 → 建空库 →
  交互式配置 → 注册服务）。详见 `deploy_client/CHK_DEPLOY.md`。客户库为**全新空库，零数据导入**。
- 我方：模型机起 vendor 服务（8355），经云中继 HTTPS 对外；防火墙只放行中继 IP；模型机开机自启 + 建议配 UPS。
- 验收清单：登录、账户管理、识别（走我方 API）、知识库（走我方 API）、客户远程调用、报告 PDF。

## 9. 公网出口方案（我方无公网服务器，推荐）

首选：**租一台最便宜的轻量云服务器做中继**（模型与数据仍在我方物理机，永不上云）。

- 中继上跑 frp（或 Caddy/nginx 反向代理），HTTPS 回传我方 8355；出口加机器令牌 + 可选 IP 白名单。
- 费用参考：轻量服务器活动价约 50~100 元/年（2C2G、3~5M 带宽足够），域名约 10~30 元/年，合计约 100~200 元/年。
- 这是无公网服务器的小厂商最常用、最稳的做法；识别场景一次上传几 MB 图片，3~5M 带宽够用。

备选：**花生壳/蒲公英商业内网穿透**（约 200~300 元/年）——免域名免服务器，但带宽与稳定性不如自建中继，只建议作过渡。

两种方案都要求我方模型机与网络 7×24 在线，断电/断网即中断服务。
