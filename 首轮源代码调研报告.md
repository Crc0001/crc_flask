# HwishAI 菌种识别系统 · 首轮源代码调研报告

> 调研日期：2026-08（首轮）　范围：全项目（`app/` 全部 Python、全部模板与 JS、`deploy_client/` 部署工具、`tests/`、根目录入口与配置）
> 方法：四个切片并行逐行审阅 + 交叉复核（认证权限 / 远程 API / 核心业务 / 前端部署），所有行号均与源码核对。
> 结论口径：按「漏洞 / 异常管理 / 代码混乱」三类，每条含 文件:行号 + 影响 + 严重级 + 修复建议。

---

## 0. 总览

| 维度 | 数量 | 其中高危 |
|---|---|---|
| 漏洞 | 约 34 项 | 7 项 |
| 异常管理 | 10 项 | — |
| 代码混乱 | 15 项 | — |

**总体判断**：鉴权骨架（登录 + 蓝图级权限 + 对象级越权 + 审计）和 SQL 注入防护（全 ORM 参数化）做得规范，但**输入校验、前端转义、部署面加固**三块存在系统性缺口。最危险的三条链：

1. **上传任意文件落盘 `static/uploads` → 同源存储型 XSS**（两端皆可，`/api/v1/recognize` 完全无扩展名检查）；
2. **前端 6 处 innerHTML 裸拼接**，其中趋势分析图例的数据源（菌种名/采样地点）是操作员可直接写入的 → 用户可控存储型 XSS；
3. **NSSM 服务以 SYSTEM 运行 + 明文 HTTP + 明文落盘全部机密 + 已知默认口令/密钥** → XSS 一旦利用即整机沦陷，且交付包内默认口令明文可见。

---

## 1. 高危漏洞（建议交付前修复）

### V-01　上传链路任意文件落盘 static/uploads → 同源存储型 XSS【高】
- `app/routes/ai_detection.py:62-67`：`mime_type.startswith('image/')` 或扩展名在白名单，**满足其一即放行**。MIME 来自客户端可伪造——上传一个 `Content-Type: image/png` 的 `x.html` 即可通过校验。
- 落盘点：client 模式 `ai_detection.py:93-95` 原样写盘；vendor 模式 `app/services/recognition.py:385-387` 在解码**之前**原样写盘。
- `app/routes/remote_api.py:142-159`：vendor `/api/v1/recognize` **完全不做扩展名/类型检查**，机器令牌持有者可直接写入任意扩展名文件。
- 全项目无 `after_request` 安全头（无 `X-Content-Type-Options: nosniff`、无 CSP），Flask static 按扩展名返回 Content-Type，`.html/.svg` 可直接同源执行脚本 → 劫持管理员会话。
- **修复**：扩展名严格白名单 + 魔数校验 + 解码成功后强制重写为 `.jpg` 再落盘（丢弃原始文件名与扩展名）；上传目录移出 static 或限制文件名；全局加 nosniff / X-Frame-Options / CSP；`MAX_CONTENT_LENGTH` 限制大小。

### V-02　前端 DOM XSS 集群（分析页图例为完全用户可控）【高/中】
- **高**：`app/static/js/analysis.js:465-477` 图例 `dataset.label` 裸拼 innerHTML 与 title。label 来自 `/analysis/data`（`app/routes/analysis.py:93-103`），即 `Sample.final_strain_name` / `collect_location`——操作员通过「录入检测报告」（`ai_detection.py:329,362`）**可直接写入任意字符串**。菌种名填 `<img src=x onerror=...>` 后，任何打开趋势分析页的人（含管理员）都会执行。
- **中**：`ai_detection.js` 同文件内 `renderOrbCandidates`（784-816 行）已用 `escapeHtml`，但以下 4 处漏掉：
  - `:458-469` MALDI 匹配结果 `strain_name/scientific_name`；
  - `:612-621` 16S 匹配结果同名两列；
  - `:1020-1051` 报告弹窗 `value="${latestSelectedStrainName}"`（属性闭合注入）与 `<textarea>${latestDetectionSummary}</textarea>`（标签闭合注入）；
  - `:1318/1362` 全局 toast `${message}`（message 多处来自服务端响应/错误文本）。
  - 这些数据源为后台维护的菌种库/分类器输出，属「有工具没用对」的防御缺失。
- **修复**：统一封装 `escapeHtml`/`apiFetch`，所有后端字段入 DOM 前转义（value/textarea/title 属性重点处理引号）。

### V-03　Windows 服务以 SYSTEM 运行 + 明文 HTTP【高】
- `deploy_client/05_install_service.bat:33` NSSM 安装从未设置 `ObjectName` → 默认以 LocalSystem（SYSTEM）运行。
- `run_client.py:23` / `run_vendor.py:23` waitress 监听 `0.0.0.0`；`app/config.py:64-66` 未设 `SESSION_COOKIE_SECURE`，登录口令与会话 Cookie 局域网明文传输。
- **修复**：创建专用低权限本地账户（`nssm set HwishAIStrain ObjectName ".\hwishai_svc" "<密码>"`），该账户仅授权 `C:\HwishAI` 必要目录；内网使用至少说明风险、建议后续上 TLS 或限定监听地址。

### V-04　已知默认凭据/密钥 + 引导账号「复活」后门【高】
- `app/config.py:55` `SECRET_KEY` 默认 `dev-secret-key-change-me`（**当前开发机 `instance/` 下无 config.py，正在使用该默认值**，会话可被离线伪造 → 全站登录绕过）；`:59` DB 默认 `root:123456`；`:85-87` 默认口令 `HwishAI@2026` / `admin123456`。
- `app/__init__.py:57-76`：每次启动只要 `hwishai`/`admin` 账号不存在就用默认口令重建（super_admin **不设** `must_change_password`）→ 删除账号 + 重启 = 后门复活；交付包 `dist/.../app/config.py` 内这些默认口令明文可见，客户可直接读到。
- `run.py:6` dev 服务器 `debug=True` 监听 0.0.0.0（Werkzeug 调试器含 PIN 控制台）。
- **修复**：生产模式（waitress）下 `SECRET_KEY` 未显式配置即拒绝启动；删除默认 DB 口令与默认 bootstrap 口令（必须显式提供）；超管引导账号也强制首登改密；引导重建加开关（如首次引导后写标记文件）；vendor 机尽快补 `instance/config.py` 随机密钥。

### V-05　机密明文落盘、无 ACL 收紧【高】
- `deploy_client/04_configure.py:65-82`：`SECRET_KEY`、机器令牌、DB 密码、管理员/超管口令全部明文写入 `C:\HwishAI\instance\config.py`（`quote_plus` 可逆，非加密），且未设置 NTFS ACL → 本机任何用户可读。
- `app/routes/remote_api.py:50-52`：vendor 侧机器令牌（`VENDOR_API_TOKENS`）明文存配置，无哈希（client 账号令牌反而是 SHA-256 哈希 + compare_digest，两端不一致）。
- **修复**：`instance/config.py` 写后收紧 ACL（仅服务账户可读）；机器令牌校验改哈希比对；文档标注该文件为敏感文件。

### V-06　上传无大小限制 / 无魔数校验 → 图像炸弹与内存 DoS【高】
- 全项目无 `MAX_CONTENT_LENGTH`：`ai_detection.py:70`、`remote_api.py:145`、`maldi_matching.py:86/243`（TXT 不限大小）、`ai_detection.py:354`（`maldi_image` 零校验直接进 `LONGBLOB`，可撑爆库）均 `file.read()` 全量入内存。
- `recognition.py:56-58` `cv2.imdecode` 解压超大尺寸图像（解压炸弹）无宽高限制。
- **修复**：`MAX_CONTENT_LENGTH`（如 20MB）；解码前校验宽高上限与魔数；`maldi_image` 复用图片校验；TXT 加大小上限。

### V-07　账号管理逻辑漏洞：停用/启用 admin 会静默降级为 operator【高】
- `app/routes/admin_accounts.py:105-121`：`update()` 里 `is_active = request.form.get("is_active") == "1"`、`role = _valid_new_role()`——**表单缺 role 字段时默认 operator**。
- `app/templates/admin/accounts.html:199-208`：启用/停用按钮只 POST `{ is_active }`。
- 后果：超级管理员点「停用/启用」某个 admin 账号 → 该账号角色被静默改成 operator（权限整体丢失，且无审计变化记录）。
- **修复**：`update()` 缺字段时保持原值（`get("role") or target.role`），停用/启用走独立端点或前端补齐 role 字段；对角色变更写审计。

---

## 2. 中危问题

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M-01 | `remote_api.py:27-30,158-159` | 识别全局 `threading.Lock` 串行 + 无超时/限流；`requests timeout=300` 单值（connect/read 不分）。一个慢请求持锁，其余识别线程全排队 → 端点整体不可用 | 改 `Semaphore(n)`+队列上限+硬超时（如 (5,120)），超限 429；识别加限流 |
| M-02 | `ai_detection.py:302,400-402,431,495,571,649`、`analysis.py:126`、`maldi_matching.py:168/284/338/375/417/537`、`remote_api.py:115`、`strain_showcase.py:199` | 异常 `str(e)` 直接回显客户端（路径/库名/堆栈片段）；`_forward_request` 把上游非 200 body 原样透传 | 对外统一友好文案，细节进 logger |
| M-03 | `health.py:34-44` | `resp.json()` 的 `ValueError` 未被捕获（except 只有 `RequestException`）→ 上游畸形响应时探活端点 500 | 单独 try/except ValueError |
| M-04 | 全站表单与 fetch | 无 CSRF Token（仅靠 `SameSite=Lax` 缓解）；登出是 GET 链接（`layout.html:123`） | 引入 CSRF token（Flask-WTF 或自实现）；敏感操作强制 POST |
| M-05 | `remote_api.py:37,52` | 令牌可从 `?token=` query 传（进日志/Referer/历史）；vendor 白名单 dict `in` 比较（非恒定时间）、无端点级 scope，一把令牌通吃 3 个端点 | 仅接受 Bearer；`hmac.compare_digest`；白名单加 scopes |
| M-06 | `auth.py:75-76`、`auth/change_password.html:70` | `must_change_password` 仅登录后重定向，且改密页有显式「暂不修改，返回系统」链接，直接访问其他页面也可绕过 | `before_request` 拦截（放行改密/登出），首次改密前不显示跳过链接 |
| M-07 | `auth.py:34-36` | `_safe_next` 只拦 `\\` 开头，`next=/\evil.com` 可绕过（浏览器把路径反斜杠归一化为 `//`）→ 开放重定向 | 拒绝包含 `\` 的 next，或强制以 `/` 开头且第二字符非 `/`、非 `\` |
| M-08 | `maldi_matching.py:459-501` | 16S 输入无长度上限，`SequenceMatcher` O(n·m) × 全库参考序列 → 算法级 DoS | 限制序列长度（如 ≤2000 bp）并缓存参考序列 |
| M-09 | `requirements*.txt`、`analysis/index.html:212-213`、`00_prepare_vendor.ps1` | 依赖仅 `>=` 未锁定；Chart.js/FontAwesome 走公网 CDN 无 SRI，**客户机离线时图表直接失效**；Python 安装器/NSSM/wheels 下载无哈希校验 | 锁 `==` 版本并留 lockfile；Chart.js 本地化；下载加 SHA256 校验 |
| M-10 | `backup_db.py`、`03_init_mysql.py` | mysqldump 全量明文落盘（含口令哈希）无加密、无目录 ACL；口令经 `MYSQL_PWD` 环境变量传递；应用账号 GRANT ALL | `--defaults-extra-file`（0600）；备份目录 ACL/加密；GRANT 收敛为业务所需 |
| M-11 | `dist/hwishai_client_deploy/` | 交付包含 `app/services/jpg_blob.py`（明文 `root/123456` + 开发机绝对路径）、`app/downloade/`、完整 vendor 模型源码；`app/config.py` 默认口令明文随包 | `build_package.ps1` 增加排除清单（jpg_blob/downloade 等） |
| M-12 | `models/user.py:115-119`、`auth.py:49-57`、`admin_accounts.py:145-147` | 锁定仅按账号（5 次错密码），无 IP/全局维度 → 可故意锁死他人账号（DoS）；密码错误分支在锁定判断**之前**，锁定期间继续试错每 5 次会重新延长 `locked_until`（可无限续锁）；管理员重置密码不清 `locked_until`（被重置用户仍处于锁定）；`auth.py:59-61`「账号已停用」提示可枚举账号状态 | 锁定检查提前；重置密码同时解锁；加 IP 维度限流与通用文案 |
| M-13 | `auth.py:67`、`config.py:64-66` | 登录成功未轮换/清理会话（弱会话固定面）；`SESSION_COOKIE_SECURE` 未设 | 登录后 `session.clear()` 再 `login_user`；HTTPS 后设 Secure |
| M-14 | `admin_console.py:61-65` | 概览页 `Sample.query.count()` 无保护（vendor 分支的 maldi_count 有 try），client 库缺表时 500 | 与 maldi_count 同样包 try/except |
| M-15 | `strain_db.py:138-142,170` | edit/delete `commit()` 无 try/rollback（与 ai_detection/maldi 不一致） | 统一 try + rollback + 友好提示 |
| M-16 | `strain_db.py:72-79`、`strain_showcase.py:225-227` | 分页 `query_string` 未 URL 编码（搜索词含 `&`/`#` 破坏链接） | `urllib.parse.urlencode` 生成 |
| M-17 | `maldi_matching.py:172-277,342-376` | 参考谱增删无任何 `audit()`；任何有 `maldi_matching` 权限的 operator 均可增删 | 增删写审计；考虑独立 `maldi_ref_write` 权限 |

---

## 3. 低危 / 加固项

| # | 位置 | 问题 |
|---|---|---|
| L-01 | `ai_detection.py:201,287`、`recognition.py:575` | 服务器绝对路径 `result_path` 回传浏览器 |
| L-02 | `ai_detection.py:480,490` | 下载文件名直接拼 `sample_code` 未清洗（控制字符/分隔符） |
| L-03 | `yolo_service.py:372` | `pickle.load` 加载模型文件（路径不可控，属隐患治理） |
| L-04 | `auth.py:49-57` | 用户不存在时跳过哈希比较 → 用户名枚举（时序） |
| L-05 | `strain_showcase.py:114-121` | `_static_image_url` 对 `http(s)://`、`//` 直接透传（依赖 DB 数据可信） |
| L-06 | `strain_db.py:117-121` | `mass_spectrum` 回显固定 `image/png` 与真实内容不符 |
| L-07 | `models/user.py:190-201` | `audit()` 吞异常且自带 commit——若在业务写未提交前调用会连带提交半成品事务 |
| L-08 | 全项目 | 无任何安全响应头（nosniff / X-Frame-Options / CSP / Referrer-Policy） |
| L-09 | `spectrum_plot.py:25,54,66,72,159-166` | 裸 `except:` 吞错；fig 未 finally close（matplotlib 内存泄漏） |
| L-10 | `ai_detection.py:39` | 白名单含 `heic/heif/webp`，与 OpenCV 解码能力不一致，且同文件 `:616` 另有一套白名单 |

---

## 4. 异常管理专项

1. **错误信息直出 `str(e)`**（M-02）——最普遍的问题，涉及 8 个文件 20+ 处。
2. **日志用 `print` 而非 logging**：`ai_detection.py:301,512-513,563,646`、`analysis.py:82,116-117`、`jpg_blob.py` 多处；Windows 服务下 stdout 进 `service.log` 无级别/轮转（NSSM 有 rotate 但无结构化）。
3. **裸 except 吞错**：`spectrum_plot.py` 4 处；`recognition.py:468`（整皿分类失败静默降级，可接受但应记日志）；`strain_db.py:52-53,138-140` 日期解析失败**静默跳过过滤条件**（错传日期得到全量结果）。
4. **写路径保护不一致**：`ai_detection.py:397-398`、`maldi_matching.py:280,371` 有 rollback；`strain_db.py` 两处 commit 无保护（M-15）。
5. **`remote_api.py:158-162`**：识别未捕获异常 → 默认 HTML 500（API 应返回 JSON）；识别失败分支不写审计（与成功分支不对称）。
6. **`health.py:40`**：`resp.json()` ValueError 未捕获（M-03）；模块级缓存 dict 无锁（低）。
7. **部署脚本失败不中断**：`05_install_service.bat` 各 nssm/netsh/powershell 命令无 errorlevel 检查、失败仍回显成功；`00_prepare_vendor.ps1:43-46` pip 非零退出不触发 Stop；`service_stop/uninstall.bat` 无存在性/结果校验；`03_init_mysql.py:73` `CREATE USER IF NOT EXISTS` 不会更新已存在账号的密码（现场坑）。
8. **前端 401 处理不统一**：仅 `ai_detection.js:691-695` 一处跳登录；`strain_db.js:237,323` 会话过期时把登录页整页 HTML 注入表格/弹窗，页面错乱无引导。
9. **`03_init_mysql.py` 密码黑名单过窄**（仅 `' " \ ;`），其余特殊字符仍可注入生成 SQL 字符串（经 mysql.exe 执行）。

---

## 5. 代码混乱专项

1. **三份代理样板重复**：`remote_api.py:93-122` / `strain_showcase.py:186-208` / `ai_detection.py:76-210` 各实现一遍 base_url/token/timeout/异常/状态码，且超时默认值 300/60/300 不一致、错误文案不一致 → 建议统一代理客户端。
2. **vendor/client 字符串分支散落** + `strain_showcase.py:181/254` 同名路由（`index`/`detail`）在同一蓝图定义两遍（运行期取其一）。
3. **`ai_detection.py:178-210` 与 `:264-299`** 两大分支近乎逐行复制（候选映射、analysis_text 拼装、audit、返回结构）。
4. **死代码**：`yolo_service.py:637-849 detect_and_draw` 无调用；语义特征/门禁整块被注释（`yolo_service.py:479-550`、`recognition.py:481,493`）；`_DEMO_INPUT_RISK`（`ai_detection.py:41-50`）仅兜底；`ai_detection.js:762-782,833-845,910-911,938-939` 注释块 + `:704-717` 与 `:972-975` 不可达分支并存；`base.js` 空文件；`analysis.html`/`ai_advice.html` 空壳模板（`lang="en"`、空 body）；`strain_db.html`+`strain_db_edit.html` 与 `strain_db/*.html`+`strain_db.js` 两套实现并存；`ai_detection.py:318` `location_hierarchy` 传模板无任何消费点；`strain_showcase.py:249-252` client 分支 404 死入口；`remote_api.py:122` 无意义三元表达式。
5. **根目录卫生**：`11.py`、`test_sql_in.py`、`sql_buchong.py`、`test_plot_generation.py`、`crop_plate_batch.py`、`eval_*.py`、`import_*.py`、`image_slicer.py`、`crc_ai.sql`、`ngrok.exe`（32MB 二进制）等一次性脚本/产物堆在仓库根目录；建议移入 `scripts/` 或删除，`ngrok.exe` 加 `.gitignore`。
6. **print 调试残留**（见异常管理 2）。
7. **import 不一致**：`analysis.py:5 from app import db`（其余均 `from app.extensions import db`，属脆弱循环导入）；`maldi_matching.py:253` 函数内重复导入；`:20` `datetime` 未使用。
8. **魔法数字散落**：置信度阈值 `0.4/0.5/0.15/0.8/0.85/0.65...`、tile `448`、超时 `300/60/3`、TTL `30`、`slice(0,3)`、`setTimeout` 等无具名常量。
9. **N+1 查询**：`sample_lite.py:29-63` 三层循环查询分类树，且每次页面加载 + 下拉接口重复全量扫表。
10. **超长函数**：`recognition.py:379-583`（205 行）、`yolo_service.py:637-849`（212 行）、`ai_detection.py:76-210`（135 行）。
11. **内联样式/脚本混杂**：`strain_db.html`、`client_index.html`、`admin/*.html` 等大段 `<style>`/`<script>` 内嵌，`admin/_nav.html` 被 4 页 include 重复输出。
12. **测试耦合真实环境**：`conftest.py:10` 直连开发库 + `test_super` session 级真实账号残留；`test_detection_flow.py` 实跑模型管线、依赖中文字体；`run_all_tests.bat` 用 marker+skipif+环境变量三套机制并存；前端 XSS 沉点与部署脚本零测试覆盖。
13. **命名不一致**：快捷方式 `AI_junzhong.url`（`05_install_service.bat:57`）与 `CHK_DEPLOY.md` 描述「AI菌种分析系统」不一致。

---

## 6. 做得好的地方（保留）

- **SQL 注入**：全项目查询走 SQLAlchemy ORM 参数化；知识库全文检索查询经 `_sanitize_fulltext_query`（`strain_showcase.py:59-70`）清洗，未见注入点。
- **鉴权骨架**：登录 + 蓝图级功能权限两道 `before_request`（`app/__init__.py:116-145`）；账号操作对象级越权校验（`_scope_error`/`_valid_new_role`，`admin_accounts.py:36-53`）基本正确；`api_access` 权限对远程调用生效。
- **令牌处理（client 侧）**：账号 API Token 以 SHA-256 哈希落库 + `hmac.compare_digest`（`models/user.py:47-53,100-104`）；重置后仅显示一次。
- **审计**：登录/检测/报告/账号/权限操作均带操作者与 IP；失败登录也留痕。
- **配置分层**：env > `instance/config.py` > 默认值；`04_configure.py` 自动生成随机 SECRET_KEY；`03_init_mysql.py` 过滤危险密码字符、用 `MYSQL_PWD` 避免命令行暴露；备份脚本保留 30 天自动清理。
- **部署隔离**：交付包排除模型权重与知识库数据；client 端不装 torch/opencv；双模式权限与页面开关（MALDI 仅 vendor）实现一致。
- **模板转义**：Jinja 自动转义为主，仅 `tojson` 安全用法，无 `|safe` 滥用。
- **测试**：vendor 65 + client 12 用例通过，专用 `test_super` 不触碰真实 `hwishai` 账号。

---

## 7. 修复优先级建议

| 批次 | 内容 | 主要位置 | 预估 |
|---|---|---|---|
| **P0（交付前）** | ① 上传链路加固（白名单+魔数+大小+强制 `.jpg` 落盘+安全头）；② 前端统一转义（analysis 图例、MALDI/16S、报告弹窗、toast）；③ NSSM 低权限服务账户；④ 密钥治理（SECRET_KEY 缺失拒启、删默认口令、超管强制改密、引导复活开关、vendor 补 instance 配置）；⑤ 修复 admin 停用降级 bug；⑥ 部署包剔除 `jpg_blob.py`/`downloade`/默认口令 | V-01~V-07 | 1~2 天 |
| **P1（本轮可做）** | 识别限流/超时与锁改造、`str(e)` 清理、healthz ValueError、令牌仅 Bearer、CSRF token、`must_change_password` 拦截、`_safe_next` 反斜杠、16S 长度上限、依赖锁定 + Chart.js 本地化、下载哈希校验、部署脚本 errorlevel、前端统一 401、strain_db rollback、MALDI 审计 | M-01~M-17 | 2~3 天 |
| **P2（清理）** | 死代码/空模板/两套 strain_db 实现、根目录脚本归置、print→logging、常量集中、N+1 修复、测试隔离（临时库+mock） | 5~6 节 | 1~2 天 |

> 本轮为纯调研，未改动任何代码。下一轮可从 P0 开始逐项修复并补回归用例。

---

## 8. P0 修复记录（已完成）

| 项 | 修复内容 | 涉及文件 |
|---|---|---|
| V-01 上传链路 | 新增统一校验 `upload_guard`（扩展名白名单 png/jpg/jpeg/bmp/webp + 魔数一致 + 头部尺寸上限 6400 万像素 + 单文件 20MB + 服务端生成安全文件名）；识别管线改为**解码成功后才落盘**；`/api/v1/recognize` 同样校验且拒绝时写审计；MALDI TXT 限 2MB；`save_detection_report`/`export_pdf`/`upload_maldi_image` 全部接入校验 | `app/services/upload_guard.py`（新增）、`app/routes/ai_detection.py`、`app/routes/remote_api.py`、`app/routes/maldi_matching.py`、`app/services/recognition.py` |
| V-01/L-08 响应头 | 全站 `nosniff` + `X-Frame-Options` + `Referrer-Policy` + CSP（中等强度）；`MAX_CONTENT_LENGTH=20MB`，413 对 API 返回 JSON（路由内 `except HTTPException: raise` 防吞） | `app/config.py`、`app/__init__.py`、`app/templates/413.html`、`404.html` |
| V-02 前端转义 | 分析页图例 label、MALDI/16S 结果表、报告弹窗（value/textarea/src）、全局 toast 全部 `escapeHtml`；`analysis.js` 新增统一转义函数 | `app/static/js/analysis.js`、`app/static/js/ai_detection.js` |
| V-03 服务账户 | 05 脚本自动创建低权限本地账户 `hwishai_svc`（随机密码、移出 Users 组、仅服务登录）并以该账户运行服务，不再以 SYSTEM 运行 | `deploy_client/05_install_service.bat` |
| V-04 密钥治理 | `SECRET_KEY` 未显式配置时改用进程级随机值（不可伪造、重启失效）+ 警告日志；生产入口 `run_client.py`/`run_vendor.py` 缺失显式 SECRET_KEY/DB/上游配置即拒绝启动；引导账号：首次引导后写 `instance/.bootstrap_done` 标记，删除账号不再自动重建；账号不存在且密码未显式配置时拒绝创建（移除内置默认口令）；`run.py` debug 改为 `FLASK_DEBUG=1` 才开启 | `app/config.py`、`app/__init__.py`、`run_client.py`、`run_vendor.py`、`run.py` |
| V-05 明文机密 | 本机新增 `instance/config.py`（gitignore 排除）显式提供随机 SECRET_KEY 等；客户机由 04_configure 生成（已有） | `instance/config.py` |
| V-07 降级 bug | `update()` 表单缺省 role 时保持原角色，杜绝"停用/启用把 admin 降级为 operator" | `app/routes/admin_accounts.py` |
| M-11 部署包瘦身 | 打包剔除 `jpg_blob.py`、`recognition.py`、`yolo_service.py`、`downloade/`；已重建 `dist/hwishai_client_deploy`（65MB，无权重/instance 泄漏）并验证 | `deploy_client/build_package.ps1` |
| 回归测试 | 新增 `tests/test_security_p0.py`（12 例：上传校验、HTML 伪装拒绝且不落盘、413 JSON、安全头、降级修复、引导标记）；vendor **77 passed**、client **12 passed** 全绿 | `tests/test_security_p0.py`、`tests/conftest.py` |

验证：开发服务器已用新代码重启（无 debug、安全头生效），`hwishai / HwishAI@2026` 登录正常。

---

## 9. P1 修复记录（已完成）

| 项 | 修复内容 | 涉及文件 |
|---|---|---|
| M-01 识别锁/超时/限流 | 识别锁改**非阻塞快速失败**（忙时 429，不做无限排队）；`requests` 超时全部拆分为 `(connect=5s, read)`；新增进程内滑动窗口限流器，应用于 `/api/v1/*`（无效令牌 30/min/IP、识别 20/min/调用方、知识库 120/min/调用方，均可配置）与登录 | `app/services/rate_limit.py`（新增）、`app/routes/remote_api.py`、`app/config.py` |
| M-02 错误信息清理 | 全部 `str(e)` 回显改为通用文案 + `logger`（含 Excel 页 HTML 注入点）；删除 `print` 调试残留 | `ai_detection.py`、`analysis.py`、`maldi_matching.py`、`remote_api.py`、`strain_showcase.py`、`health.py` |
| M-03 healthz | `resp.json()` ValueError 单独捕获（上游畸形响应不再打挂探活）；缓存加锁防击穿 | `app/routes/health.py` |
| M-04 CSRF | 会话级 CSRF Token：后端 `_csrf_protect` 全局校验（表单域/`X-CSRF-Token` 头，恒定时间比较）；前端 layout 全局 fetch 包装自动注入 + 统一 401/登录页重定向处理；登录/改密表单加隐藏域；`/api/*` 与 remote_api 豁免（SameSite=Lax + Token 覆盖） | `app/__init__.py`、`app/templates/layout.html`、`auth/login.html`、`auth/change_password.html`、`tests/conftest.py`（测试客户端自动注入） |
| M-05 令牌加固 | 移除 `?token=` 查询串回退（仅 Bearer）；vendor 白名单改 `hmac.compare_digest` 恒定时间比较；支持端点级 scope（`{"name":..., "scopes":[...]}`，兼容旧字符串格式） | `app/routes/remote_api.py` |
| M-06 强制改密 | 全局 before_request 拦截：`must_change_password` 账号除改密/登出/静态资源外一律重定向改密页（API 返回 403 JSON）；改密页强制期隐藏"暂不修改"链接 | `app/__init__.py`、`auth/change_password.html` |
| M-07 开放重定向 | `_safe_next` 拒绝任何含反斜杠的 next（防 `/\evil.com` 归一化绕过） | `app/routes/auth.py` |
| M-08 16S DoS | 查询序列长度上限 2000bp；参考序列进程内 60s 缓存（避免每请求全量清洗） | `app/routes/maldi_matching.py` |
| M-09 供应链/离线 | requirements 全部锁定 `==` 精确版本；Chart.js 本地化（`app/static/js/chart.umd.min.js` v4.4.9，客户机离线可用）；移除 FontAwesome CDN（图标改 emoji）；`00_prepare_vendor.ps1` 对 Python 安装包与 NSSM 加 SHA256 校验 + pip 退出码检查 + wheel 数量校验 | `requirements*.txt`、`app/static/js/chart.umd.min.js`、`analysis/index.html`、`analysis.js`、`deploy_client/00_prepare_vendor.ps1` |
| M-10 备份/DB 口令 | `backup_db.py` 改用 `--defaults-extra-file`（0600 临时文件，用完即删）替代 `MYSQL_PWD`；`03_init_mysql.py` GRANT 收敛为业务最小集（去掉 ALL PRIVILEGES）；`05` 收紧 `instance\` ACL、`06` 收紧 `backups\` ACL 且备份任务以 SYSTEM 运行 | `deploy_client/backup_db.py`、`03_init_mysql.py`、`05_install_service.bat`、`06_install_backup_task.bat` |
| M-12 锁定机制 | 锁定检查提前（锁定期间试错不再延长 `locked_until`）；重置密码同时清锁；停用账号登录文案统一（防枚举）；登录失败限流（每 IP+用户名 60 次/5min，可配置） | `app/routes/auth.py`、`app/routes/admin_accounts.py`、`app/config.py` |
| M-13 会话 | 登录成功轮换会话（保留 CSRF Token 跨登录边界）；`SESSION_COOKIE_SECURE` 环境变量开关（内网 HTTP 默认关，接入 HTTPS 中继后置 1） | `app/routes/auth.py`、`app/config.py` |
| M-14 概览容错 | 管理概览 sample 统计查询包 try/except（client 缺表时不再 500） | `app/routes/admin_console.py` |
| M-15 写回滚 | strain_db 编辑/删除 commit 加 rollback + 友好提示 | `app/routes/strain_db.py` |
| M-16 分页链接 | `query_string` 改 `urllib.parse.urlencode` 编码 | `app/routes/strain_db.py`、`app/routes/strain_showcase.py` |
| M-17 MALDI 参考谱 | 参考谱增/删仅限管理员角色（操作员 403），增删均写审计日志 | `app/routes/maldi_matching.py` |
| E-07 部署脚本 | `05_install_service.bat` 全部 nssm set/启动加 errorlevel 检查；`service_stop/uninstall` 存在性与结果校验；`01_install_python.bat` 校验已装版本为 3.12 | `deploy_client/*.bat` |

回归测试：新增 `tests/test_security_p1.py`（16 例）；vendor **92 passed / 1 skipped**、client **12 passed** 全绿。
端到端验证：无 CSRF 的登录 POST → 403；带 Token → 302；会话保持正常；部署包已重建（65.2MB，含本地 Chart.js）。

> 遗留（P2）：死代码/模板清理、根目录脚本归置、N+1 修复、测试隔离（临时库）、
> `SESSION_COOKIE_SECURE` 待接入 HTTPS 中继后置 1、审计日志保留策略。

---

## 10. P2 清理记录（已完成）

| 项 | 内容 | 涉及文件 |
|---|---|---|
| 死代码（vendor 管线） | 删除 `detect_and_draw`（213 行无调用）、`_get_feature_statistics`/`_get_semantic_text_features`/`_feature_distance_signal`/`_semantic_signal`/`_compose_input_risk` 五个仅被注释引用的函数及 SEMANTIC 提示词/缓存全局变量；`_classify_images` 内注释掉的"门禁"代码块全部移除（yolo_service 849 → 377 行） | `app/services/yolo_service.py` |
| 死代码（前端） | 删除 ai_detection.js 注释掉的拒答/风险面板代码、不可达的 `latestInputAccepted` 分支及相关变量 | `app/static/js/ai_detection.js` |
| 死模板/文件 | 删除未被路由引用的 `strain_db.html`/`strain_db_edit.html`（保留 `strain_db/` 重构版）、空壳 `analysis.html`/`ai_advice.html`、空 `base.js`、无引用的 `analysis_result.png`；首页移除"AI 建议"死菜单项 | `app/templates/`、`app/static/js/` |
| 死参数 | 移除检测页模板死参数 `location_hierarchy`（下拉数据走 `/api/location_data`） | `app/routes/ai_detection.py` |
| 根目录卫生 | 删除 `11.py`/`test_sql_in.py`/`sql_buchong.py`/`test_plot_generation.py`/`crc_ai.sql`（旧库 dump）；维护脚本（eval_*/import_*/update_*/image_slicer）与 SQL DDL、文档归置到 `scripts/`（已补 sys.path 引导与根目录相对路径修正）；`ngrok.exe` 移入 `tools/` 并加入 `.gitignore` | 仓库根目录、`scripts/`、`tools/` |
| 模块归位 | `crop_plate_batch.py` 移入 `app/services/`，`recognition.py` 与评测脚本导入同步更新 | `app/services/crop_plate_batch.py` |
| import 一致性 | `analysis.py` 的 `from app import db` 改为 `from app.extensions import db` | `app/routes/analysis.py` |
| N+1 修复 | `SampleLite.get_hierarchical_data()` 改单次 DISTINCT 查询 + 内存组装 | `app/models/sample_lite.py` |

回归：vendor **92 passed / 1 skipped**、client **12 passed** 全绿；识别管线测试通过（模块移动无回归）；
JS 三文件 `node --check` 语法通过；开发服务器已重启验证（登录/检测/分析页 200）；
部署包重建（65.2MB，旧模板与 base.js 已不随包）。

> 本轮未做（需另行决策）：测试隔离（临时库/mock，改动测试基架较大）、
> 审计日志保留策略（建议加定期清理）、`SESSION_COOKIE_SECURE` 待 HTTPS 中继就绪后置 1。



